// AMD vs UMD vs CommonJS vs ESM (ES6)
// ESMs are always file based. One file, one module.
// ESMs are singletons. When you import instance of it will be created and when second import happens somewhere in the code it will just reference original instances that was created first time.
